@model Obligatorio.LogicaAplicacion.Dtos.Pagos.PagosDtoAlta

@{
    ViewData["Title"] = "Reporte por Monto Superior";
}

<div class="row justify-content-center mt-5">
    <div class="col-md-6">
        <div class="card shadow-sm border-0 rounded">
            <div class="card-body">
                <h2 class="text-center mb-4 text-uppercase fw-semibold">Generar Reporte por Monto</h2>

                <form asp-action="UsuarioPagoSuperior">
                    <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                    <div class="mb-3">
                        <label asp-for="MontoPago" class="form-label fw-bold">Monto de pago</label>
                        <input asp-for="MontoPago" class="form-control" />
                        <span asp-validation-for="MontoPago" class="text-danger"></span>
                    </div>

                    <div class="d-grid mt-4">
                        <button type="submit" class="btn btn-primary">Generar Reporte</button>
                    </div>
                    <p>@ViewBag.Mensaje</p>
                </form>

                <div class="mt-3 text-center">
                    <a asp-action="Index" class="text-decoration-none">← Volver al listado</a>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}